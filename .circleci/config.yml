version: 2.1

jobs:
  test_app:
    docker:
      - image: circleci/node:15
    parallelism: 2
    steps:
      - checkout
      - run:
          name: Get dependencies
          command: npm install
      - run:
          name: Run tests
          command: npm run test
      - store_test_results:
          path: ./junit.xml
      - store_artifacts:
          path: ./junit.xml

workflows:
  test:
    jobs:
      - test_app:
          filters:
            branches:
              only:
                - main


